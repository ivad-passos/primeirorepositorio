#include <stdio.h>
#include "ARVORE.h"

int main() {
  tp_arvore arvore;
  arvore = inicializa_arvore();
  insere_no(&arvore,6);
  insere_no(&arvore,3);
  insere_no(&arvore,9);
  printf("%d", arvore->)
  
}